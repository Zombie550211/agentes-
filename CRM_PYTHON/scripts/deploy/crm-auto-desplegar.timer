# Cada minuto comprueba si hay un commit nuevo en main (una consulta ligera a
# GitHub; solo descarga y despliega cuando hay cambios).
[Unit]
Description=CRM: buscar commits nuevos en main cada minuto

[Timer]
OnBootSec=2min
OnUnitActiveSec=60s
AccuracySec=5s

[Install]
WantedBy=timers.target
